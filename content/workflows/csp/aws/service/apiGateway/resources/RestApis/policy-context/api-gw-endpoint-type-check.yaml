schema: 1
type: policy-context
 
id: api-gw-endpoint-type-check
name: API-GW-ENDPOINT-TYPE-CHECK
labels:
  - contextualization
 
csp: aws
service: apiGateway
resource-type: RestApis
 
class: risk
subclass: config
category: uncategorized
subcategory: best-practice
 
filters:
  - vendor: aws-config
    policies:
      - api-gw-endpoint-type-check
    match-type: any
    rules:
      - .vendorId == "aws-config" and (.vendorPolicy |contains ("api-gw-endpoint-type-check"))
 
risk-config:
  default-risk: ""
