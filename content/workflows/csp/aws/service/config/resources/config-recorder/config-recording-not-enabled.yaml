schema: 1
type: policy-context

id: config-recording-not-enabled
name: Config recording not enabled
labels:
  - contextualization

csp: aws
service: config
resource-type: config-recorder

class: risk
subclass: config
category: visibility
subcategory: logging

filters:

risk-config:
  default-risk: medium
