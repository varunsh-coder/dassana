schema: 1.0
type: policy-context
id: "ebs-snapshot-is-public"
name: "EBS snapshot is public"
labels:
  - contextualization

category: access-sharing
subcategory: public-access
csp: aws
service: ec2
resource-type: snapshot

filters:
  - vendor: aws-config
    policies:
      - "ebs-snapshot-public-restorable-check"
    match-type: any
    rules:
      - .normalizerId == "aws-config" and (.vendorPolicy |contains ("ebs-snapshot-public-restorable-check"))
  - vendor: prisma-cloud
    policies:
      - "7c714cb4-3d47-4c32-98d4-c13f92ce4ec5"
    match-type: any
    rules:
      - .normalizerId == "prisma-cloud" and .vendorPolicy == "7c714cb4-3d47-4c32-98d4-c13f92ce4ec5"

risk-config:
  default-risk: critical