schema: 1
type: policy-context
 
id: ec2-managedinstance-platform-check
name: EC2-MANAGEDINSTANCE-PLATFORM-CHECK
labels:
  - contextualization
 
csp: aws
service: ec2
resource-type: instance
 
class: risk
subclass: config
category: uncategorized
subcategory: best-practice
 
filters:
  - vendor: aws-config
    policies:
      - ec2-managedinstance-platform-check
    match-type: any
    rules:
      - .vendorId == "aws-config" and (.vendorPolicy |contains ("ec2-managedinstance-platform-check"))
 
risk-config:
  default-risk: ""
