schema: 1
type: policy-context
 
id: ec2-stopped-instance
name: EC2-STOPPED-INSTANCE
labels:
  - contextualization
 
csp: aws
service: ec2
resource-type: instance
 
class: risk
subclass: config
category: uncategorized
subcategory: best-practice
 
filters:
  - vendor: aws-config
    policies:
      - ec2-stopped-instance
    match-type: any
    rules:
      - .vendorId == "aws-config" and (.vendorPolicy |contains ("ec2-stopped-instance"))
 
risk-config:
  default-risk: medium
