schema: 1
type: policy-context
 
id: ebs-in-backup-plan
name: EBS-IN-BACKUP-PLAN
labels:
  - contextualization
 
csp: aws
service: ec2
resource-type: volume
 
class: risk
subclass: config
category: storage
subcategory: availability
 
filters:
  - vendor: aws-config
    policies:
      - ebs-in-backup-plan
    match-type: any
    rules:
      - .vendorId == "aws-config" and (.vendorPolicy |contains ("ebs-in-backup-plan"))
 
risk-config:
  default-risk: ""
