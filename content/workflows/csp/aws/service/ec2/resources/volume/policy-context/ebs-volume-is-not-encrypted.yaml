schema: 1.0
type: policy-context
id: "ebs-volume-is-not-encrypted"
name: "EBS snapshot is public"
labels:
  - contextualization

category: cryptography
subcategory: encryption
csp: aws
service: ec2
resource-type: volume

filters:
  - vendor: aws-config
    policies:
      - "ec2-ebs-encryption-by-default"
      - "encrypted-volumes"
    match-type: any
    rules:
      - .normalizerId == "aws-config" and (.vendorPolicy |contains ("ec2-ebs-encryption-by-default"))
      - .normalizerId == "aws-config" and (.vendorPolicy |contains ("encrypted-volumes"))
  - vendor: prisma-cloud
    policies:
      - "7c714cb4-3d47-4c32-98d4-c13f92ce4ec5"
    match-type: any
    rules:
      - .normalizerId == "prisma-cloud" and .vendorPolicy == "7c714cb4-3d47-4c32-98d4-c13f92ce4ec5"

risk-config:
  default-risk: medium